import 'package:flutter/material.dart';

class RestaurantFragment extends StatelessWidget {
  const RestaurantFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Restaurant")));
  }
}
